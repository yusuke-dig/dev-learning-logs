# 2026-04-21 Swift Day03: オプショナル

## 今日の作業内容

- Day03: オプショナル (Optional / if let / guard let) を実装・完了
- `safeDivide`, `firstElement`, `parseAge` の3関数を実装
- 全7テスト合格

## 今日学んだこと

### Optional の基本

`Int?` は "Int または nil" を表す型。値があるかどうか不明なときに使う。

### guard let による早期リターン

```swift
guard let age = Int(s), (0...120).contains(age) else { return nil }
return age
```

`guard` の条件はカンマで複数並べられる。変換と範囲チェックを1行にまとめられて簡潔。`if-else` より「正常系だけ下に続く」構造になり読みやすい。

### Array.first は自動で Optional

`Array.first` は空配列に対して自動で `nil` を返す (`Element?` 型)。`isEmpty` チェックは不要。
